"""A module for translating between alignments and edits sequences."""


def align(x: str, y: str, edits: str) -> tuple[str, str]:
    return ("", "")


def edits(x: str, y: str) -> str:
    return ""


def local_align(chrom: str, pos: int, pat: str, edits: str) -> tuple[str, str]:
    return ("", "")
